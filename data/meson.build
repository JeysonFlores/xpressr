symbolic_icons = ['ionicons-add-circle']

foreach i : symbolic_icons
    install_data(
        join_paths('icons', 'symbolic', i + '-symbolic.svg'),
        install_dir: join_paths(get_option('datadir'), 'icons', 'hicolor', 'symbolic', 'apps')
    )
endforeach
